[package]
name = "xtask"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
rimr-bindings = { path = "../crates/rimr-bindings", features = ["export"] }
